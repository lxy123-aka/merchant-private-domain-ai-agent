# S3 数据结构与接口定义文档

## 一、核心数据结构

### 1. 门店商家信息（Shop Profile）

门店画像信息是 Agent 生成个性化内容的基础上下文，包含门店基础属性与长期记忆摘要。

```json
{
  "shop_id": "shop_001",
  "shop_name": "味美火锅",
  "shop_type": "餐饮-火锅",
  "city": "重庆",
  "customer_price": 128,
  "customer_price_unit": "元/人",
  "target_customer": "家庭聚餐、朋友聚会",
  "store_size": "200平米，8张桌",
  "history_activities": [
    {
      "activity_id": "act_20260501",
      "title": "国庆开业大促",
      "type": "满减活动",
      "date": "2026-10-01",
      "result": "单日营收提升45%，新增会员120人"
    },
    {
      "activity_id": "act_20260618",
      "title": "双十一会员日",
      "type": "会员专属",
      "date": "2026-11-11",
      "result": "会员复购率提升18%"
    }
  ],
  "brand_tone": "热情、接地气、有烟火气",
  "create_time": "2026-03-15T10:00:00Z",
  "update_time": "2026-08-10T15:30:00Z"
}
```

**字段说明：**

| 字段 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| `shop_id` | string | 是 | 门店唯一标识，全局唯一 |
| `shop_name` | string | 是 | 门店名称 |
| `shop_type` | string | 是 | 门店品类，枚举值：餐饮-火锅/餐饮-烧烤/烘焙/茶叶/特产等 |
| `city` | string | 是 | 所在城市，用于本地化案例匹配 |
| `customer_price` | number | 是 | 客单价（元），用于活动定价参考 |
| `target_customer` | string | 否 | 目标客群描述 |
| `history_activities` | array | 否 | 历史活动记录，最多保留最近20条，用于生成内容时参考过往策略 |
| `brand_tone` | string | 否 | 品牌调性标签，用于控制文案语气风格 |

### 2. 对话记录（Dialog Record）

记录每轮商家与 Agent 的交互内容，支撑短期对话上下文与长期记忆摘要。

```json
{
  "dialog_id": "dlg_20260817_001",
  "shop_id": "shop_001",
  "session_id": "sess_a1b2c3d4",
  "turn": 3,
  "user_input": "帮我写3条小龙虾朋友圈，突出麻辣鲜香",
  "user_intent": "文案生成",
  "slot_filling": {
    "content_type": "朋友圈",
    "quantity": 3,
    "theme": "小龙虾-麻辣"
  },
  "agent_output": "1. 【麻辣上头！这锅小龙虾，香到隔壁桌来问链接...】...",
  "rag_references": [
    {
      "case_id": "case_20260401",
      "title": "某川菜馆小龙虾节运营方案",
      "source": "有赞-餐饮行业案例库"
    }
  ],
  "model_version": "deepseek-v3.5",
  "chat_time": "2026-08-17T15:30:00Z",
  "feedback": null
}
```

**字段说明：**

| 字段 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| `dialog_id` | string | 是 | 对话记录唯一标识 |
| `shop_id` | string | 是 | 关联门店ID，用于多门店数据隔离 |
| `session_id` | string | 是 | 会话标识，同一轮对话的多次交互共享此ID |
| `turn` | integer | 是 | 对话轮次编号，从1开始递增 |
| `user_input` | string | 是 | 商家原始输入文本 |
| `user_intent` | string | 是 | 意图分类结果，枚举：活动策划/文案生成/案例查询/策略咨询 |
| `slot_filling` | object | 否 | 槽位填充结果，包含提取的关键参数 |
| `agent_output` | string | 是 | Agent 生成的回复内容 |
| `rag_references` | array | 否 | 本轮生成所引用的 RAG 检索结果，用于溯源 |
| `model_version` | string | 是 | 使用的模型版本，便于后续效果对比 |
| `chat_time` | string | 是 | 对话时间，ISO 8601 格式 |
| `feedback` | string/null | 否 | 商家反馈（点赞/点踩），用于后续优化 |

### 3. 运营案例（Operation Case）

RAG 知识库中的案例文档，存储有赞运营 SOP 与本地行业成功案例，经向量化后支持语义检索。

```json
{
  "case_id": "case_20260401",
  "industry": "餐饮-火锅",
  "sub_category": "节假日营销",
  "title": "某川渝火锅店小龙虾节私域运营全案",
  "summary": "通过朋友圈预热+社群秒杀+到店核销三步走，单周新增会员300+，营收提升60%",
  "content": "完整运营方案正文...",
  "key_metrics": {
    "新增会员数": 300,
    "营收提升比例": "60%",
    "ROI": "1:4.5"
  },
  "tags": ["小龙虾", "节假日", "私域", "社群秒杀"],
  "source": "有赞-餐饮行业案例库",
  "source_url": "https://www.youzan.com/cases/xxx",
  "vector_id": "vec_a1b2c3d4e5f6",
  "create_time": "2026-03-15T10:00:00Z",
  "update_time": "2026-07-20T08:00:00Z"
}
```

**字段说明：**

| 字段 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| `case_id` | string | 是 | 案例唯一标识 |
| `industry` | string | 是 | 所属行业分类，用于 RAG 检索时的过滤条件 |
| `sub_category` | string | 否 | 子分类（如：节假日营销/日常运营/会员激活） |
| `title` | string | 是 | 案例标题 |
| `summary` | string | 是 | 案例摘要（100字以内），用于快速预览 |
| `content` | string | 是 | 完整运营方案正文 |
| `key_metrics` | object | 否 | 关键数据指标，便于后续做效果对比参考 |
| `tags` | array | 否 | 标签数组，支持多维度检索 |
| `source` | string | 是 | 来源标识，如"有赞-餐饮行业案例库" |
| `source_url` | string | 否 | 原始来源链接，便于人工查阅 |
| `vector_id` | string | 是 | 向量数据库中的向量ID，用于检索结果回链 |

### 4. 任务拆解结果（Task Plan）

Plan-and-Solve 模块输出的结构化子任务列表，每个子任务可独立执行。

```json
{
  "plan_id": "plan_20260817_001",
  "shop_id": "shop_001",
  "original_request": "帮小龙虾店策划周末引流活动",
  "tasks": [
    {
      "task_id": "task_001",
      "task_type": "rag_search",
      "description": "检索餐饮行业周末引流成功案例",
      "params": {"industry": "餐饮", "keyword": "周末引流"},
      "dependencies": [],
      "status": "completed",
      "output_format": "json"
    },
    {
      "task_id": "task_002",
      "task_type": "llm_generate",
      "description": "结合客单价制定活动策略与定价方案",
      "params": {"shop_type": "餐饮-火锅", "customer_price": 128},
      "dependencies": ["task_001"],
      "status": "pending",
      "output_format": "markdown"
    },
    {
      "task_id": "task_003",
      "task_type": "llm_generate",
      "description": "撰写朋友圈预热文案（3条）",
      "params": {"quantity": 3, "tone": "接地气"},
      "dependencies": ["task_002"],
      "status": "pending",
      "output_format": "markdown"
    }
  ],
  "create_time": "2026-08-17T15:30:00Z"
}
```

---

## 二、接口定义

### 2.1 对话发起接口

商家通过自然语言发起运营需求，Agent 完成意图识别、任务规划、内容生成并返回结果。

| 项目 | 说明 |
| :--- | :--- |
| **接口路径** | `POST /api/chat` |
| **Content-Type** | `application/json` |
| **角色** | 核心入口，所有商家操作均走此接口 |

**请求参数：**

| 参数名 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| `shop_id` | string | 是 | 门店唯一标识 |
| `query` | string | 是 | 商家自然语言输入（支持普通话及方言口语，最长2000字） |
| `session_id` | string | 否 | 会话标识，不传则系统自动生成新会话 |
| `stream` | boolean | 否 | 是否开启流式输出，默认 `false` |

**响应参数：**

| 参数名 | 类型 | 说明 |
| :--- | :--- | :--- |
| `dialog_id` | string | 本次对话记录ID |
| `result_text` | string | Agent 生成的运营内容（文案/方案/话术） |
| `task_list` | array | 自动拆分的子任务列表（复杂需求时返回） |
| `rag_sources` | array | 引用的 RAG 检索来源列表，用于溯源 |
| `intent` | string | 识别到的意图类型 |
| `confidence` | number | 意图识别置信度（0~1） |
| `follow_up` | string | 系统主动追问（当槽位缺失时） |

**响应示例：**

```json
{
  "dialog_id": "dlg_20260817_001",
  "result_text": "以下是为您策划的小龙虾周末引流活动方案...",
  "task_list": [
    {"task_id": "task_001", "status": "completed", "output": "..."},
    {"task_id": "task_002", "status": "completed", "output": "..."}
  ],
  "rag_sources": [
    {"case_id": "case_20260401", "title": "某川渝火锅店小龙虾节运营全案"}
  ],
  "intent": "活动策划",
  "confidence": 0.95,
  "follow_up": ""
}
```

### 2.2 知识库检索接口

根据行业与关键词检索匹配的运营案例，供 Agent 在生成内容时引用。

| 项目 | 说明 |
| :--- | :--- |
| **接口路径** | `POST /api/case/search` |
| **Content-Type** | `application/json` |
| **角色** | Agent 内部调用，也可供商家直接查询案例 |

**请求参数：**

| 参数名 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| `industry` | string | 是 | 行业分类（如"餐饮-火锅"） |
| `keyword` | string | 是 | 检索关键词（如"周末引流""会员激活"） |
| `top_k` | integer | 否 | 返回结果数量，默认 5，最大 20 |
| `shop_id` | string | 否 | 门店ID，用于优先返回同品类案例 |

**响应参数：**

| 参数名 | 类型 | 说明 |
| :--- | :--- | :--- |
| `case_list` | array | 匹配的案例数组，按相关度降序排列 |
| `total` | integer | 匹配总数 |
| `search_time_ms` | integer | 检索耗时（毫秒），用于性能监控 |

**响应示例：**

```json
{
  "case_list": [
    {
      "case_id": "case_20260401",
      "title": "某川渝火锅店小龙虾节运营全案",
      "summary": "通过朋友圈预热+社群秒杀+到店核销三步走...",
      "match_score": 0.92,
      "source": "有赞-餐饮行业案例库"
    }
  ],
  "total": 1,
  "search_time_ms": 45
}
```

### 2.3 读取门店记忆接口

获取门店画像信息与近期对话记录，供 Agent 在生成内容时注入上下文。

| 项目 | 说明 |
| :--- | :--- |
| **接口路径** | `GET /api/memory/get` |
| **Content-Type** | `application/json` |
| **角色** | Agent 内部调用，在每次生成前读取门店上下文 |

**请求参数：**

| 参数名 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| `shop_id` | string | 是 | 门店唯一标识 |
| `recent_count` | integer | 否 | 返回近期对话记录条数，默认 10 |

**响应参数：**

| 参数名 | 类型 | 说明 |
| :--- | :--- | :--- |
| `shop_info` | object | 门店画像信息（见"门店商家信息结构体"） |
| `recent_chat` | array | 近期对话记录，按时间倒序排列 |
| `memory_summary` | string | 长期记忆摘要（按月维度压缩），用于控制 Context Window 消耗 |

**响应示例：**

```json
{
  "shop_info": {
    "shop_id": "shop_001",
    "shop_name": "味美火锅",
    "shop_type": "餐饮-火锅",
    "customer_price": 128,
    "brand_tone": "热情、接地气"
  },
  "recent_chat": [
    {
      "dialog_id": "dlg_20260817_001",
      "user_input": "帮我写3条小龙虾朋友圈",
      "agent_output": "1. 【麻辣上头！...}",
      "chat_time": "2026-08-17T15:30:00Z"
    }
  ],
  "memory_summary": "该门店为重庆火锅店，客单价128元，近期主打小龙虾品类，品牌调性热情接地气。"
}
```

---

## 三、接口错误码规范

| 错误码 | 说明 | 处理建议 |
| :--- | :--- | :--- |
| `200` | 请求成功 | — |
| `400` | 参数校验失败 | 检查 shop_id、query 等必填字段 |
| `404` | 门店不存在 | 确认 shop_id 是否正确 |
| `429` | 请求频率超限 | 降级为排队等待，提示商家稍后再试 |
| `500` | 服务端异常 | 返回兜底话术，记录错误日志 |
| `503` | 大模型服务不可用 | 触发降级策略，返回预设话术 |

---

## 四、数据安全与权限控制

- **数据隔离**：所有接口按 `shop_id` 做数据隔离，确保 A 门店无法访问 B 门店数据。
- **敏感信息脱敏**：日志中禁止明文记录商家手机号、地址等敏感信息。
- **内容合规**：所有输出内容经过敏感词过滤器，拦截违反《广告法》的极限词（如"最""第一""国家级"）。
- **审计追踪**：每次 RAG 检索与内容生成均记录 `case_id` 与 `dialog_id`，支持全链路溯源。
