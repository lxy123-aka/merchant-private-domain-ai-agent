# S4 核心功能实现说明、关键代码片段 & 自测记录

## 一、核心模块实现说明
项目核心基于自研 Agent 调度逻辑，调用 DeepSeek API 完成推理。重点实现意图识别、任务拆分、RAG 检索联动、对话上下文管理。目前已完成核心调度逻辑开发，前端交互页面处于迭代阶段。

## 二、关键代码片段（Python）

```python
# Agent核心调度主逻辑
class MerchantAgent:
    def __init__(self, shop_id):
        self.shop_id = shop_id
        self.memory = MemoryModule(shop_id)
        self.rag = RAGSearch()

    def run(self, user_query):
        # 1. 读取门店历史记忆
        shop_info = self.memory.get_shop_info()
        
        # 2. 意图识别
        intent = self.intent_recognize(user_query)
        case_context = ""
        
        # 3. 需要案例则执行RAG检索
        if intent.need_case:
            case_context = self.rag.search(shop_info["shop_type"], user_query)
        
        # 4. 复杂任务自动拆解
        task_list = []
        if intent.is_complex:
            task_list = self.task_split(user_query)
        
        # 5. 组装Prompt，调用DeepSeek
        prompt = self.build_prompt(user_query, shop_info, case_context, task_list)
        reply = call_deepseek(prompt)
        
        # 6. 保存对话记录
        self.memory.save_chat(user_query, reply)
        
        return {"reply": reply, "tasks": task_list}
```
三、自测记录
测试用例 1
输入：帮小龙虾店策划周末引流活动
预期：输出完整活动方案，自动检索餐饮同行案例
自测结果：通过；成功拆分活动预热、到店福利、朋友圈宣传子任务
测试用例 2
输入：写3条蛋糕店新品宣传朋友圈
预期：产出3条风格不同适合私域转发文案
自测结果：通过；输出文案适配烘焙门店场景
测试用例 3（方言测试）
输入：帮我想哈茶叶店老客户复购活动
预期：识别川渝方言指令，正常生成方案
自测结果：通过；能够识别口语化方言表述
四、现存优化方向
方言识别提示词持续优化
RAG 案例相似度匹配精度优化
后续完成 Streamlit 前端页面联调
